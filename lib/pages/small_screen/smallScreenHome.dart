import 'package:flutter/material.dart';

class SmallScreenHome extends StatelessWidget {
  const SmallScreenHome({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}